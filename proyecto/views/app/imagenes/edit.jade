extends ../../layout.jade

block contenido 
    div(class="col-md-6 remove-float center-block big-top-space")
    h1 Editar imagen
        form(action="/app/imagenes/#{imagen.id}?_method=PUT", method="POST")
            div(class="form-group")
                label(for="title") Titulo
                input(type="text", name="title",placeholder="Título de la imagen...", id="title", 
                class="form-control", value="#{imagen.title}")
            div 
            div(class="row top-space")
                div(class="col-xs-12 col-sm-6")
                    input(type="submit" value="Guardar cambios" class="btn btn-info")